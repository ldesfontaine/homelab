---
# Source de vérité — profils WireGuard ADMIN-RELAY (peers app).
#
# Ce fichier est consommé par scripts/wg-admin-gen-profile.py.
# Les clés privées ne sont JAMAIS ici — elles sont lues à la volée depuis
# ~/homelab-keys/wg-admin-relay/<peer>.key.
#
# Pour la config OPNsense (instance WG-ADMIN-CLIENT côté maison), voir
# docs/wg-admin-profiles.md (UI OPNsense, pas un .conf).

hub:
  endpoint: "wg-hub.ldesfontaine.com:51821"
  public_key: "87VrPX34/AJ22jYmRL9WdrQXZBhEKEgaMO/qfELmyiM="
  dns: "10.10.10.1"   # Unbound interne via le tunnel

# AllowedIPs canonique — voir docs/wg-admin-profiles.md pour la doctrine.
# 10.99.10.0/24 = subnet WG admin (peers entre eux)
# 10.10.10.0/24 = MGMT (OPNsense, Proxmox UI)
# 10.10.60.0/24 = SVC_PRIV (Authentik, Vaultwarden, Filebrowser, Traefik interne)
# 10.10.70.0/24 = LAB
# 10.10.99.0/24 = BACKUP (Pi5 PBS futur)
peers:
  - name: laptop
    tunnel_ip: "10.99.10.10/32"
    allowed_ips: "10.99.10.0/24, 10.10.10.0/24, 10.10.60.0/24, 10.10.70.0/24, 10.10.99.0/24"
    devices:
      - "ParrotOS laptop Lucas (NetworkManager profile: homelab)"

  - name: phone
    tunnel_ip: "10.99.10.11/32"
    allowed_ips: "10.99.10.0/24, 10.10.10.0/24, 10.10.60.0/24, 10.10.70.0/24, 10.10.99.0/24"
    devices:
      - "iPhone Lucas (WireGuard iOS app)"

  - name: tablet
    tunnel_ip: "10.99.10.12/32"
    allowed_ips: "10.99.10.0/24, 10.10.10.0/24, 10.10.60.0/24, 10.10.70.0/24, 10.10.99.0/24"
    devices:
      - "Tablette (à provisionner)"
