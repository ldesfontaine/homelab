---
# ===========================================================================
# Rôle `portfolio` — fichier chapeau qui include les sous-fichiers thématiques
# ===========================================================================
# Référence : docs/adr/ADR-000-fondations-ansible.md (Décision 6)
# ===========================================================================

- name: Prepare portfolio directories and Docker network
  ansible.builtin.include_tasks: prep.yml
  tags:
    - portfolio
    - portfolio-prep

- name: Deploy portfolio compose and start container
  ansible.builtin.include_tasks: deploy.yml
  tags:
    - portfolio
    - portfolio-deploy

# Pas de tâche backup host-side : les snapshots Payload sont déclenchés
# manuellement via /admin (cf. defaults/main.yml et le repo termfolio).
