---
# ===========================================================================
# Constantes internes au rôle `portfolio`
# ===========================================================================
# Préfixées par `_` selon la convention ADR-000 Décision 2.
# ===========================================================================

_portfolio_compose_file: "{{ portfolio_compose_dir }}/docker-compose.yml"
_portfolio_env_file: "{{ portfolio_compose_dir }}/.env"
_portfolio_container_name: portfolio
