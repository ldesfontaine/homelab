---
galaxy_info:
  author: ldesfontaine
  description: Deploy portfolio container via docker compose pulling the image from GHCR
  license: MIT
  min_ansible_version: '2.17'
  platforms:
    - name: Debian
      versions:
        - trixie
  galaxy_tags:
    - homelab
    - portfolio
    - docker
    - compose
    - web

# ADR-000 Décision 2 : pas de dépendances inter-rôles, l'ordre est piloté
# par les playbooks. Le rôle `docker` doit avoir été joué AVANT.
dependencies: []
