---
galaxy_info:
  author: ldesfontaine
  description: Install fail2ban with sshd jail on the homelab custom port
  license: MIT
  min_ansible_version: '2.17'
  platforms:
    - name: Debian
      versions:
        - trixie
  galaxy_tags:
    - system
    - homelab
    - security
    - fail2ban
    - bruteforce

# Décision 2 de l'ADR-000 : pas de dépendances inter-rôles, l'ordre est
# piloté par les playbooks.
dependencies: []
