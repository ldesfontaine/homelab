---
galaxy_info:
  author: ldesfontaine
  description: Deploy Pangolin (Traefik-integrated reverse proxy) with Cloudflare DNS-01 Let's Encrypt
  license: MIT
  min_ansible_version: '2.17'
  platforms:
    - name: Debian
      versions:
        - trixie
  galaxy_tags:
    - homelab
    - pangolin
    - traefik
    - reverseproxy
    - docker
    - compose
    - letsencrypt

# ADR-000 Décision 2 : pas de dépendances inter-rôles, l'ordre est piloté
# par les playbooks. Les rôles `docker` et `ufw_docker` doivent avoir été
# joués AVANT (cf. playbooks/deploy-vps-services.yml).
dependencies: []
