---
# ===========================================================================
# Rôle `docker` — fichier chapeau qui include les sous-fichiers thématiques
# ===========================================================================
# Référence : docs/adr/ADR-000-fondations-ansible.md (Décision 2)
#             docs/adr/ADR-002-firewall-strategy.md
# ===========================================================================

- name: Install Docker CE from the official Docker apt repository
  ansible.builtin.include_tasks: install.yml
  tags:
    - docker
    - docker-install

- name: Configure Docker daemon
  ansible.builtin.include_tasks: configure.yml
  tags:
    - docker
    - docker-config

- name: Manage docker group membership
  ansible.builtin.include_tasks: users.yml
  tags:
    - docker
    - docker-users
