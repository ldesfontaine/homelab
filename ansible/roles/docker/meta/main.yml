---
galaxy_info:
  author: ldesfontaine
  description: Install Docker CE from the official Docker apt repository and configure the daemon
  license: MIT
  min_ansible_version: '2.17'
  platforms:
    - name: Debian
      versions:
        - trixie
  galaxy_tags:
    - system
    - homelab
    - docker
    - container

# ADR-000 Décision 2 : pas de dépendances inter-rôles, l'ordre est piloté
# par les playbooks.
dependencies: []
