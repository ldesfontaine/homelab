---
galaxy_info:
  author: ldesfontaine
  description: Base OS configuration — timezone, locale, NTP, essential packages
  license: MIT
  min_ansible_version: '2.17'
  platforms:
    - name: Debian
      versions:
        - trixie
  galaxy_tags:
    - system
    - homelab
    - common

# Décision 2 de l'ADR-000 : pas de dépendances inter-rôles, l'ordre est piloté
# par les playbooks.
dependencies: []
