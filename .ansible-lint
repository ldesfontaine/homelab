---
profile: production

exclude_paths:
  - .venv/
  - .cache/
  - .collections/

# On laisse les warnings dans le profil prod, mais on tolère les warnings expérimentaux
warn_list:
  - experimental

skip_list:
  # On gère sans surveys-mode, autorisé pour homelab solo
  - role-name[path]
